org: konflux-ci
default_config: orgs/konflux-ci-default-config.yaml

exclude:
  - .github
  - .fullsend
  - community-operators-prod   # 64k files, too large for agentready
  - tests-common-packages      # empty repo, clone fails
